import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="规约/React/hooks" />

# hooks 使用规则 插件依赖检察 底层原理

# Don't mirror props in state

# ref 使用 函数式组件获取 ref(forwardRef)

# 变量提升

# React key 强制刷新

# 事件映射

# immer use-immer

<!-- if else
enum as const -->
